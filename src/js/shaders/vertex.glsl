/* vertex shader:
- basically a program that will run for every vertex throughout our geometry
- responsible for placing all the vertices in the correct position to create all objects */

void main() {
    gl_Position = vec4(1, 0, 0, 1);
}
